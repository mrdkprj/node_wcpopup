{
  "name": "test",
  "version": "1.0.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "start": "tsc ./main.ts --esModuleInterop --lib ES2015,dom && electron .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "electron": "^32.1.0",
    "typescript": "^5.5.3"
  }
}
